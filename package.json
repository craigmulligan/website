{
  "name": "hobochild.tk",
  "version": "1.0.0",
  "scripts": {
    "build": "mdown -i'./pages/**/*.md' -o ./out --header ./assets/header.html",
    "serve": "serve ./out -p 4000",
    "deploy:prep": "cp keybase.txt out/keybase.txt && cp .nojekyll out/.nojekyll && cp CNAME out/CNAME",
    "deploy": "yarn deploy:prep && gh-pages -d out --dotfiles"
  },
  "author": "hobochild",
  "license": "ISC",
  "devDependencies": {
    "gh-markdown-cli": "^0.2.0",
    "gh-pages": "^1.1.0",
    "serve": "^11.2.0"
  }
}
